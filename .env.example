# Supabase (auto-configured by Lovable Cloud)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# =====================================================
# IMPORTANT: The secrets below are stored securely in
# Lovable Cloud and accessible ONLY in Edge Functions.
# 
# For LOCAL development, create a .env.local file
# (which is gitignored) and add your keys there.
# =====================================================

# Gemini API (used in edge functions)
# GEMINI_API_KEY="your-gemini-api-key"

# Google OAuth (used in edge functions for auth)
# GOOGLE_AUTH_CLIENT_ID="your-google-client-id"
# GOOGLE_AUTH_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth (used in edge functions for auth)
# GITHUB_AUTH_CLIENT_ID="your-github-client-id"
# GITHUB_AUTH_CLIENT_SECRET="your-github-client-secret"
